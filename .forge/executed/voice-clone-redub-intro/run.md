<!-- forge-slug: voice-clone-redub-intro -->
# 실행 노트 — 내 목소리 클론으로 입문 영상 재더빙 (pptx 재빌드)

실행: 2026-06-30 · 방식: 직접 실행(사용법 `tts` 파이프라인 재사용) · 사용법이 활성 슬롯 점유 중이라 executed/에 파킹(공존)

## 계획대로 된 것
- 44슬라이드 렌더 + 44노트 재작성(시청자 구어체) + 44 클론 합성(RTF ~0.28, 24.0분 분량) + 재타이밍·조립.
- 산출 `forge-입문-발표-내목소리.mp4`(1920×1080 H.264 + AAC + 한국어 soft 자막, 24.0분), 자막 398블록, 음량 -16 LUFS.
- 원본 `forge-입문-발표.mp4` 미변경(63,402,200바이트). 새 파일로만 산출.
- 전부 로컬·외부 전송 0 (ADR-0001 승계).

## 계획과 달랐던 것 / 현장 결정
1. **렌더 — PowerPoint 불필요**: 기존 `forge-입문-발표.pdf`(44p)를 `pdftoppm`로 PNG 변환. 사용법 회고의 PowerPoint AppleScript 렌더 통증을 통째로 회피(긍정적 우회).
2. **장면전환 0건**: 원본 영상이 크로스페이드라 임계 0.1에서도 슬라이드 경계 미검출 → "원본 비디오 재타이밍" 불가 확인 → pptx 재빌드로 선회(그릴링 단계 결정).
3. **내레이션 = 노트 1:1 재작성**: SRT 307블록→44슬라이드 퍼지 매핑을 피하고, 노트 44개를 전담 에이전트로 단일 패스 재작성(문체 일관성). 코칭 지시어 제거 확인.
4. **파이프라인 일반화 중 경로 버그 2건(수정)**:
   - `sv.py`가 import 시점에 `{cwd}/GPT_SoVITS/eres2net`를 path에 추가 → 일반화 스크립트가 chdir을 import 뒤에 둬 `ERes2NetV2` 미발견. **chdir을 import 앞으로** 이동해 해결.
   - CLI 인자 상대경로가 chdir 후 깨짐 → **인자 경로를 chdir 전에 절대화**로 해결. 두 수정 모두 사용법 `run.sh` 재사용도 더 견고하게 함.
5. **-16 LUFS 조립 내장**: 사용법은 별도 재믹스로 음량을 키웠으나, 이번엔 `assemble.py`의 최종 믹스에 `loudnorm I=-16` 내장 → 한 번에 표준 음량(별도 단계 불필요). 사용법 학습의 코드화.
6. **상태**: 사용법 활성 슬롯 점유 → 입문은 `.forge/executed/voice-clone-redub-intro/`에 파킹(회고·봉인 대기). 사용자 결정(입문 바로 쌓기).

## 검증
- 자동: 1920×1080·H.264·AAC·한국어 자막 트랙·길이 1442.8초·44음성 무누락·음량 -16.1dB·원본 미변경 — 전부 통과.
- 육안/청취(UAT): 클론 정체성·동기·가독·한국어 자연스러움·음량 — **사용자 통과**.
